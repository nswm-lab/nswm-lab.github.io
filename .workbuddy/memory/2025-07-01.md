# 2025-07-01

## BadVLA 视频演示完整修复

**问题**：之前 6 个视频 URL 404 导致黑屏，原因是 URL 路径猜测错误（如 `goal/yellow_mug/trigger.mp4` 实际应为 `goal/mug/...mp4`，`spatial/pixel_block` 实际拼写为 `spatial/piexl_block`）。

**解决方案**：
1. 直接从 `badvla-project.github.io` 页面 HTML 中提取了全部 27 个视频的实际 URL
2. 下载了所有缺失的 15 个视频到 `images/badvla/`（总计 22 个本地 mp4 文件）
3. 更新 `about.md` 展示完整 7 个场景，每个 LIBERO 场景 4 列，Google Robot 合并为 4 列网格

**完整场景列表**：
- LIBERO Goal（4 视频：Normal, Pixel Block, Red Stick, Yellow Mug）
- LIBERO Long（4 视频）
- LIBERO Object（4 视频）
- LIBERO Spatial（4 视频）
- Google Robot（6 视频：Move Near, Pick Coke Can, Pick Object 各 2 个）

**关键发现**：项目网站 URL 路径规律为 `static/images/figures/{场景}/{触发器}/{文件名}.mp4`，注意 spatial 场景的 pixel_block 目录名有拼写错误 `piexl_block`。

## 隐藏导航栏深色模式切换图标

在 `_includes/masthead.html` 的 `#theme-toggle` li 元素上添加 `style="display:none;"` 隐藏太阳图标，代码保留不删除避免 bug。

## 添加 6 篇代表性成果展示到主页

在 BadVLA 卡片下方添加了论文网格（2 列），展示 6 篇与原生安全世界模型概念相关的论文：
1. **ToolHijacker** (NDSS 2026) - Agent 工具选择劫持
2. **BadToken** (CVPR 2025) - MLLM Token 级后门
3. **Merge Hijacking** (ACL 2025) - LLM 模型合并后门
4. **Transferable Attackers** (NeurIPS 2025) - LVLM 可迁移对抗攻击
5. **LVLM Attacks Survey** (IEEE TNNLS 2025) - LVLM 攻击综述
6. **GUI-World** (ICLR 2025) - GUI 世界理解基准

图片素材下载到 `images/papers/`，来源于 arXiv HTML、GitHub 仓库、PDF 渲染。每个卡片包含框架图、标签、标题、作者、摘要（关联原生安全世界模型概念）、外部链接。CSS 使用 `.lab-papers-grid` 2 列网格，响应式在 1024px 以下变为单列。

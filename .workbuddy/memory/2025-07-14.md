
## 论文展示区视觉优化（第二轮）

- 引导段落 `.lab-papers-intro` 文字颜色从 `rgba(255,255,255,0.78)` 加深为 `#e2e8f0`
- 论文卡片从**黑底白字**全面改为**白底深色字**风格：
  - 卡片背景：`linear-gradient(深蓝灰)` → `#ffffff`（纯白）
  - 标题颜色：`#f1f5f9` → `#0f172a`（深黑）
  - 作者颜色：`rgba(255,255,255,0.7)` → `#64748b`（灰蓝）
  - 描述文字：`rgba(255,255,255,0.82)` → `#334155`（深灰蓝）
  - 链接按钮：`#5eead4` → `#0d9488`（teal-600，适配白底）
  - 强调色内联样式：`#5eead4` → `#0d9488`
  - 阴影：从深色重阴影改为轻柔白卡片阴影
  - 边框：从 teal 半透明改为 slate 灰色半透明
- 构建成功，无错误

## 论文展示区视觉优化（第三轮 - 根因修复）

- **根因分析**：论文展示区所在 section 背景为 `.lab-section-dark` → `var(--gray-50)` = `#f8fafc`（近白色），之前用浅色文字在这个背景上对比度极低
- 引导段落 `.lab-papers-intro`：文字颜色 `#e2e8f0`（浅灰）→ `#334155`（深灰蓝），strong 强调色 `#5eead4` → `#0d9488`，背景改为浅 teal 渐变 `linear-gradient(135deg, #f0fdfa, #f8fafc)`
- 卡片边界清晰化：
  - 边框从 `rgba(203,213,225,0.5)` 加深为 `#cbd5e1`（slate-300 实色）
  - 图片区域底色从 `#f8fafc` 改为 `#f1f5f9`（更明显的浅灰）
  - 图片与文字分隔线从 `1px solid rgba(...)` 加粗加深为 `2px solid #e2e8f0`
  - 阴影加重：`0 4px 20px rgba(0,0,0,0.06)` + `0 1px 3px rgba(0,0,0,0.08)`
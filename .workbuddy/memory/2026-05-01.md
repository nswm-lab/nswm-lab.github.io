# 2026-05-01

## Section 2 布局优化
- 修复 Section 2（实验室主任 & 核心研究理念）左右卡片不等高问题：`.lab-about-grid` 从 `align-items: start` 改为 `align-items: stretch`
- 左侧深色卡片改为 flex 布局，highlight 框用 `margin-top: auto` 推到底部
- "全球首创"标签与标题间距从 `1rem` 缩小到 `0.6rem`
- "全球首创 · 首创者" 用词重复，改为"全球首创"

## Hero 区域间距优化
- badge `margin-bottom` 从 `1.5rem` 缩小到 `1rem`
- 英文标题 `margin-bottom` 从 `2rem` 缩小到 `1rem`
- 描述段 `margin-bottom` 从 `2.5rem` 缩小到 `1.8rem`
- 右侧卡片改为 flex 填满高度，links 用 `margin-top: auto` 推到底部对齐
